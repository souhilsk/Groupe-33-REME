import statsmodels.formula.api as smf

# 1. Filtrer les données avant 2016
pre_df = df[df['year'] < 2016].copy()

# 2. Régression avec interaction
model_parallel = smf.ols("log_crime ~ year * group", data=pre_df).fit()

# 3. Résultats
print(model_parallel.summary())
